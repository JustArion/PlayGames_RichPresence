# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Manual_Release --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Manual_Release

on:
  workflow_dispatch:
    inputs:
      Version:
        description: "Version"
        required: true

permissions:
  contents: write

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
          lfs: true
      - name: 'Cache: ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props', '**/packages.lock.json') }}
      - name: 'Run: TaggedRelease'
        run: ./build.cmd TaggedRelease
        env:
          Version: ${{ github.event.inputs.Version }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: bin'
        uses: actions/upload-artifact@v5
        with:
          name: bin
          path: bin
      - name: 'Publish: velopack'
        uses: actions/upload-artifact@v5
        with:
          name: velopack
          path: velopack
